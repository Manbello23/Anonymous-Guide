:root{
  --bg:#191009; /* deep brown */
  --paper:#F6F0E6; /* soft cream */
  --gold:#C7A97B;
  --muted:#8c7a67;
  --text:#EDE6DA;
  --max-width:760px;
  --narrow:520px;
  --radius:12px;
  --shadow: 0 8px 30px rgba(0,0,0,0.6);
  --ease: cubic-bezier(.2,.9,.25,1);
  --accent: rgba(199,169,123,0.08);
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: "Crimson Pro", Georgia, serif;
  background:linear-gradient(180deg,var(--bg), #0f0b07 80%);
  color:var(--text);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.6;
  display:flex;
  align-items:stretch;
  justify-content:center;
  padding:28px 18px;
}

/* site container */
#site{width:100%;max-width:980px;min-height:560px;position:relative;}

/* HERO / COVER */
.scene{
  display:flex;
  align-items:center;
  justify-content:center;
  flex-direction:column;
  min-height:560px;
  text-align:center;
  gap:18px;
}
.cover-wrap{transform-origin:center; transition:transform .9s var(--ease);}
.journal-cover{
  width:360px;
  height:460px;
  border-radius:10px;
  background: linear-gradient(180deg, #2a1e16 0%, #1a130c 100%);
  box-shadow: var(--shadow);
  border:1px solid rgba(255,255,255,0.02);
  display:flex;
  align-items:flex-end;
  justify-content:center;
  padding:28px;
  position:relative;
  overflow:hidden;
  isolation:isolate;
}
.journal-cover::after{
  content:"";
  position:absolute; inset:0;
  background: linear-gradient(180deg, rgba(199,169,123,0.02), transparent 40%);
  pointer-events:none;
}
.title .main{
  font-family: "Cormorant Garamond", "Playfair Display", serif;
  font-size:20px;
  line-height:1.05;
  color:var(--gold);
  letter-spacing:0.6px;
  text-align:center;
  text-transform:none;
}
.title .sub{
  margin-top:8px;
  font-size:12px;
  color:var(--muted);
  text-transform:uppercase;
  letter-spacing:1.6px;
}

/* hero single line and button */
.hero-line{
  color:var(--paper);
  max-width:640px;
  margin-top:14px;
  font-size:16px;
  opacity:0.95;
}
.btn-gold{
  margin-top:12px;
  background:transparent;
  color:var(--gold);
  border:1px solid var(--gold);
  padding:10px 22px;
  border-radius:999px;
  cursor:pointer;
  font-weight:600;
  letter-spacing:0.3px;
  transition:all .24s;
  backdrop-filter: blur(4px);
}
.btn-gold:hover{transform:translateY(-3px); box-shadow: 0 10px 30px rgba(0,0,0,0.5);}

/* hidden class */
.hidden{display:none}

/* App layout */
#app{background:rgba(255,255,255,0.02); border-radius:14px; overflow:hidden; box-shadow:var(--shadow);}

/* topbar */
.topbar{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:16px 20px;
  border-bottom:1px solid rgba(255,255,255,0.02);
}
.brand{font-family: "Cormorant Garamond", serif; color:var(--gold); font-size:15px}
.hamburger{background:transparent;border:0;color:var(--paper);font-size:20px;cursor:pointer}

/* menu */
.menu{
  position:absolute; right:24px; top:72px; z-index:60;
  width:220px; border-radius:10px; padding:12px; background:rgba(11,8,6,0.85);
  box-shadow:0 8px 30px rgba(0,0,0,0.6);
  border:1px solid rgba(255,255,255,0.02);
}
.menu ul{list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:8px}
.menu button{background:transparent; color:var(--text); border:0; text-align:left; padding:8px 6px; cursor:pointer; font-size:14px}

/* main content area */
.reader-area{padding:24px; max-width:var(--max-width); margin:0 auto}

/* views */
.view{max-width:var(--max-width); margin:0 auto; padding:28px 12px}
.lead{color:var(--paper); font-size:16px; margin-bottom:14px; max-width:760px}

/* index grid */
.grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(78px,1fr)); gap:10px; margin-top:14px}
.index-card{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent); padding:12px; border-radius:8px; text-align:center; cursor:pointer; border:1px solid rgba(255,255,255,0.02)}
.index-card.locked{opacity:0.45; filter:grayscale(0.2)}
.index-card.unlocked{border:1px solid rgba(199,169,123,0.18)}

/* day reader */
.day-reader{max-width:var(--narrow); margin:20px auto 50px; background:var(--paper); color:#1b120b; padding:26px; border-radius:10px; box-shadow: 0 10px 40px rgba(0,0,0,0.4)}
.day-header{display:flex; align-items:center; justify-content:space-between; gap:12px; margin-bottom:12px}
.day-title{font-family:"Cormorant Garamond", serif; font-size:18px; color:var(--bg)}
.nav.small{background:transparent;border:0;color:var(--muted);cursor:pointer}
.ayah{background:linear-gradient(180deg, rgba(199,169,123,0.02), transparent); padding:10px; border-radius:8px; margin-bottom:16px}
.arabic{font-family: "Scheherazade New", "Amiri", serif; font-size:20px; text-align:center; margin-bottom:8px}
.translation{font-size:13px; color:var(--muted); text-align:center}

/* content */
.content{font-size:16px; color:#261a12; line-height:1.8; margin-bottom:18px}

/* reflection */
.reflection label{display:block; font-weight:600; margin-bottom:8px}
textarea#reflectionInput{width:100%; min-height:120px; border-radius:8px; padding:12px; border:1px solid rgba(0,0,0,0.06); background:#fff; resize:vertical; font-family:inherit}
.ref-actions{display:flex; gap:8px; margin-top:8px}
.btn{padding:8px 12px; border-radius:8px; border:1px solid rgba(0,0,0,0.06); cursor:pointer; background:#fff}
.btn.red{background:#fff; color:#b83b3b; border-color:#f5cfcf}

/* locked overlay */
.locked{position:absolute; inset:0; display:flex; align-items:center; justify-content:center}
.lock-card{background:var(--bg); color:var(--text); padding:28px; border-radius:12px; text-align:center; width:360px; box-shadow:var(--shadow)}
.lock-icon{font-size:28px; margin-bottom:12px}
.lock-text{color:var(--paper); margin-bottom:12px}

/* reflections list */
.notes-list{display:flex; flex-direction:column; gap:12px}
.note-card{background:linear-gradient(180deg, #120b07, #1a130c); padding:12px; border-radius:8px; color:var(--paper)}
.note-card .meta{font-size:12px; color:var(--muted)}

/* footer */
.footer{padding:18px; text-align:center; color:var(--muted); font-size:13px}

/* responsive */
@media (max-width:640px){
  .journal-cover{width:280px;height:380px}
  .day-reader{padding:18px}
  body{padding:16px}
  .grid{grid-template-columns:repeat(4,1fr)}
}
